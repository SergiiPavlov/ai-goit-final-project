# Server
PORT=4001
NODE_ENV=development
LOG_LEVEL=info

# CORS (comma-separated). PR-03 uses per-project allowlist from DB.
# This env is a fallback allowlist for non-project endpoints (health/version) and local tooling.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Database (PostgreSQL)
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/leleka_ai

# Prisma shadow database (recommended for local dev to avoid requiring CREATEDB on DATABASE_URL user).
SHADOW_DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/leleka_ai_shadow

# Rate limit (per projectKey + IP)
RATE_LIMIT_WINDOW_SEC=60
RATE_LIMIT_MAX=60

# LLM provider
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_TIMEOUT_MS=25000
OPENAI_TEMPERATURE=0.2
